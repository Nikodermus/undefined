@require './components/*'
@require './helpers/*'
@require './library/*'


:root
  $scrollbar

// General Elements Styling
html
  background: white
  min-height: 100vh
  overflow-x: hidden
  color: black
  max-width: 100vw
  font-family: $font-main, $font-sans
  body
    font-family: @font-family
    max-width: @max-width
    min-height: 100%
    background: white
    overflow-x: hidden
    width: 100vw
    color: black
    $scrollbar
    *
      font-family: @font-family
      line-height: 1.3
      $scrollbar













